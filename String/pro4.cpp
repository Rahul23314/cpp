 // at()function:- we pass the index as the parameter to at()function and it returns ref-to-character index in the string 
 #include<iostream>
using namespace std;
int main()
{
 string s1="Hello Good morning "; 
 cout<<s1<<endl;;
 cout<<"Element at 2nd index is "<<s1.at(2);  // objectname.at(index);
return 0;
}

